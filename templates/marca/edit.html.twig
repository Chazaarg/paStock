{% extends"index.html.twig" %}

{% block title %}
Editar {{marca.nombre}}
{% endblock %}

{% block body %}
    <h1>Editar {{marca.nombre}}</h1>

    {{ include('marca/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('marca_index') }}">back to list</a>

    {{ include('marca/_delete_form.html.twig') }}
{% endblock %}
